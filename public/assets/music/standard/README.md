# Standard-tier reel music

Drop 1-4 royalty-free MP3s here (upbeat house / lo-fi, per the reel-spec template — used for listings under $600k).

`electron/main/listings/video/musicBed.ts` picks a track from this folder at render time; if it's empty, reels render silent-except-narration instead of failing.
